# v0.7.0 验收门槛

- 短任务：Direct，0 child，0 Reviewer。
- 中任务：Lite，最多 1 child，normal 模型档位，0 独立 Reviewer。
- 长而高度耦合的单范围任务：Direct，保留父会话上下文。
- `--compact` 必须保留状态、档位、预算、模型、审批和下一步，并显著小于完整 JSON。
- 热模型健康缓存不得再次执行 entitlement 模型探针。
- Git 审计不得读取所有 clean tracked 文件内容。
- RunStore 事件追加不得随历史事件总数线性重读 JSONL。
- 四平台 typecheck、完整测试和安装包重建必须通过。
